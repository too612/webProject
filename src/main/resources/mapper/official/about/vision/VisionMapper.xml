<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.main.app.official.about.vision.VisionMapper">

    <!-- TODO: Vision 전용 테이블 확정 후 SQL 교체 -->
    <select id="selectVision" resultType="com.main.app.official.about.vision.dto.VisionDto">
        SELECT
            1::BIGINT AS id,
            'Vision' AS title,
            'Vision content template' AS content
    </select>

    <insert id="insertVision" parameterType="com.main.app.official.about.vision.dto.VisionRequest">
        SELECT 1
    </insert>

    <update id="updateVision">
        SELECT 1
    </update>

    <update id="deleteVision">
        SELECT 1
    </update>

</mapper>
