<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.main.app.official.about.history.HistoryMapper">

    <!-- TODO: History 전용 테이블 확정 후 SQL 교체 -->
    <select id="selectHistory" resultType="com.main.app.official.about.history.dto.HistoryDto">
        SELECT
            1::BIGINT AS id,
            'History' AS title,
            'History content template' AS content
    </select>

    <insert id="insertHistory" parameterType="com.main.app.official.about.history.dto.HistoryRequest">
        SELECT 1
    </insert>

    <update id="updateHistory">
        SELECT 1
    </update>

    <update id="deleteHistory">
        SELECT 1
    </update>

</mapper>
