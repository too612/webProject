<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.main.app.official.news.mission.MissionMapper">
    <select id="selectMission" resultType="com.main.app.official.news.mission.dto.MissionDto">
        SELECT 'Mission' AS title, 'Mission content template' AS content
    </select>
    <insert id="insertMission" parameterType="com.main.app.official.news.mission.dto.MissionRequest">SELECT 1</insert>
    <update id="updateMission" parameterType="com.main.app.official.news.mission.dto.MissionRequest">SELECT 1</update>
    <delete id="deleteMission" parameterType="String">SELECT 1</delete>
</mapper>