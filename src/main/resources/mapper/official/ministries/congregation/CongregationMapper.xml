<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.main.app.official.ministries.congregation.CongregationMapper">

    <!-- TODO: congregation 전용 테이블 확정 후 SQL 교체 -->
    <select id="selectCongregation" resultType="com.main.app.official.ministries.congregation.dto.CongregationDto">
        SELECT
            1::BIGINT AS id,
            'Congregation' AS title,
            'Congregation content template' AS content
    </select>

    <insert id="insertCongregation" parameterType="com.main.app.official.ministries.congregation.dto.CongregationRequest">
        SELECT 1
    </insert>

    <update id="updateCongregation">
        SELECT 1
    </update>

    <update id="deleteCongregation">
        SELECT 1
    </update>

</mapper>