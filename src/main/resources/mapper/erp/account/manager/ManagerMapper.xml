<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.main.app.erp.account.manager.ManagerMapper">

    <select id="selectOfferingList" resultType="com.main.app.erp.account.manager.dto.ManagerDto$Offering">
        SELECT offering_id, member_id, member_name, amount, offering_type,
               offering_date, description, input_by, reg_date
        FROM erp_offering
        <where>
            <if test="keyword != null and keyword != ''">AND (member_name LIKE '%' || #{keyword} || '%' OR offering_type LIKE '%' || #{keyword} || '%')</if>
        </where>
        ORDER BY offering_date DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countOfferingList" resultType="long">
        SELECT COUNT(*) FROM erp_offering
        <where>
            <if test="keyword != null and keyword != ''">AND (member_name LIKE '%' || #{keyword} || '%' OR offering_type LIKE '%' || #{keyword} || '%')</if>
        </where>
    </select>

</mapper>

