<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.main.app.erp.account.budget.BudgetMapper">

    <select id="selectBudgetList" resultType="com.main.app.erp.account.budget.dto.BudgetDto$Budget">
        SELECT budget_id, budget_year, category, planned_amount, description, status
        FROM erp_budget
        <where>
            <if test="year != null">AND budget_year = #{year}</if>
        </where>
        ORDER BY budget_year DESC, category
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countBudgetList" resultType="long">
        SELECT COUNT(*) FROM erp_budget
        <where>
            <if test="year != null">AND budget_year = #{year}</if>
        </where>
    </select>

</mapper>
