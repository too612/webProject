<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.main.app.erp.account.expense.ExpenseMapper">

    <select id="selectExpenseList" resultType="com.main.app.erp.account.expense.dto.ExpenseDto$Expense">
        SELECT expense_id, expense_date, category, amount, description, approved_by, reg_date
        FROM erp_expense
        <where>
            <if test="keyword != null and keyword != ''">AND (category LIKE '%' || #{keyword} || '%' OR description LIKE '%' || #{keyword} || '%')</if>
        </where>
        ORDER BY expense_date DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countExpenseList" resultType="long">
        SELECT COUNT(*) FROM erp_expense
        <where>
            <if test="keyword != null and keyword != ''">AND (category LIKE '%' || #{keyword} || '%' OR description LIKE '%' || #{keyword} || '%')</if>
        </where>
    </select>

</mapper>
