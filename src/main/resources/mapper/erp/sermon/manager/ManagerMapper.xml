<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.main.app.erp.sermon.manager.ManagerMapper">

    <select id="selectManagerList" resultType="com.main.app.erp.sermon.manager.dto.ManagerDto$Worship">
        SELECT worship_id, title, preacher, scripture, worship_date, worship_type,
               video_url, description, status, reg_date
        FROM erp_worship
        <where>
            <if test="keyword != null and keyword != ''">AND (title LIKE '%' || #{keyword} || '%' OR preacher LIKE '%' || #{keyword} || '%')</if>
        </where>
        ORDER BY worship_date DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countManagerList" resultType="long">
        SELECT COUNT(*)
        FROM erp_worship
        <where>
            <if test="keyword != null and keyword != ''">AND (title LIKE '%' || #{keyword} || '%' OR preacher LIKE '%' || #{keyword} || '%')</if>
        </where>
    </select>

</mapper>
